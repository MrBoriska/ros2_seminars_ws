# Индивидуальные задания повышенной сложности (на оценку «Отлично» — 54 балла)

В данном документе представлены темы и технические требования к **индивидуальным практическим проектам**, позволяющим получить максимальную оценку (**54 балла**) за каждый из учебных модулей курса робототехники в ROS 2 Humble.

---

## 🏛️ Регламент модульно-рейтинговой системы

Семестровый курс разделен на **два контрольных модуля**:

```
                              ┌────────────────────────────────────────────────────────┐
                              │             Итоговая оценка за семестр                 │
                              │           (Среднее арифметическое модулей)             │
                              └───────────────────────────┬────────────────────────────┘
                                                          │
                       ┌──────────────────────────────────┴──────────────────────────────────┐
                       ▼                                                                     ▼
    ┌──────────────────────────────────────┐                              ┌──────────────────────────────────────┐
    │     МОДУЛЬ 1 (Семинары 0 и 1)        │                              │     МОДУЛЬ 2 (Семинары 2 и 3)        │
    ├──────────────────────────────────────┤                              ├──────────────────────────────────────┤
    │ Темы: Linux, Docker, ROS 2 Core,     │                              │ Темы: Gazebo Classic, ODE динамика,  │
    │ URDF, XACRO, кинематика мобильных    │                              │ KUKA YouBot (Mecanum + 5-DOF рука),  │
    │ роботов, TF2, навигация в симуляторе.│                              │ сенсоры, мобильная манипуляция.      │
    ├──────────────────────────────────────┤                              ├──────────────────────────────────────┤
    │ Стандартный трек: Семинары 0 и 1     │                              │ Стандартный трек: Семинары 2 и 3     │
    │ ➔ Оценка от 35 до 45 баллов.         │                              │ ➔ Оценка от 35 до 45 баллов.         │
    │                                      │                              │                                      │
    │ Продвинутый трек:                    │                              │ Продвинутый трек:                    │
    │ 1 Индивидуальное задание Модуля 1    │                              │ 1 Индивидуальное задание Модуля 2    │
    │ ➔ 54 БАЛЛА ЗА МОДУЛЬ 1               │                              │ ➔ 54 БАЛЛА ЗА МОДУЛЬ 2               │
    └──────────────────────────────────────┘                              └──────────────────────────────────────┘
```

### Правила получения оценки «Отлично» (54 балла):
1. Выполнение индивидуального задания по модулю **освобождает от необходимости сдавать базовые лабораторные отчеты** по соответствующим семинарам.
2. Чтобы получить 54 балла за весь семестр, студент выполняет **2 индивидуальных задания**:
   * Одно задание из **Блока Модуля 1** (сдается в середине семестра).
   * Одно задание из **Блока Модуля 2** (сдается в конце семестра).
3. Номер варианта в каждом модуле определяется строго по номеру студенческого билета / зачетной книжки (`STUDENT_ID`):
   $$\text{Вариант} = (\text{STUDENT\_ID} \pmod{15}) + 1$$
4. Все физические и кинематические параметры задачи рассчитываются по индивидуальным формулам от `STUDENT_ID`.

---

## 🛡️ Требования к валидации индивидуальных заданий

> [!IMPORTANT]
> Данные требования относятся **исключительно к индивидуальным заданиям на 54 балла**! Для стандартных отчетов по семинарам (на оценку до 45 баллов) запись rosbag не требуется — там достаточно скриншотов и листингов.

Учитывая удаленный формат работы, оценка **54 балла** за индивидуальный проект выставляется при выполнении двух условий:

1. **Индивидуализация параметров по номеру студенческого билета (`STUDENT_ID`)**:
   * Все числовые параметры задачи (радиусы, линейные скорости, дистанции упреждения и амплитуды) рассчитываются по формулам, приведенным в начале каждого блока заданий.
   * Если в коде или симуляции использованы чужие или произвольные числа — работа отклоняется.

2. **Обязательный бинарный архив ROS 2 Bag (`.db3` или `.mcap`)**:
   * Для подтверждения реального запуска симуляции к отчету прикладывается записанный лог топиков длительностью **не менее 30 секунд**:
     ```bash
     ros2 bag record /tf /clock /cmd_vel /odom /youbot/joint_states -o my_run.bag
     ```
   * *Почему этого достаточно:* Текстовые нейросети не способны сгенерировать бинарный файл базы данных SQLite3 с сотнями тысяч сериализованных сообщений, содержащих реальный микросекундный шум физического движка ODE. Записанный росбаг служит 100% подтверждением самостоятельного запуска кода студентом.

---

# 📦 Блок заданий Модуля 1 (по материалам Семинаров 1 и 2)

**Базовая платформа:** Дифференциальный колесный робот ([`basic_mobile_robot`](src/basic_mobile_robot/)) в ROS 2 и Gazebo Classic.  
**Среда выполнения:** ROS 2 Humble, `rclpy`, RViz2, Gazebo Classic.  
**Основные топики робота:** `/cmd_vel` (скорость тележки), `/odom` (одометрия), `/scan` (2D-лидар), `/joint_states` (состояния колес), `/tf` (координатные фреймы).

> [!IMPORTANT]
> **Принцип выполнения:** Все алгоритмы реализуются студентом **самостоятельно с нуля на Python (`rclpy` + `math`/`numpy`)** без использования готовых высокоуровневых библиотек навигации (Nav2 и т.д.). Решение строится на базовой геометрии, тригонометрии, дифференциальных уравнениях и формулах регуляторов.

### Индивидуальные параметры (рассчитываются каждым студентом):
```python
STUDENT_ID = 20240185  # Ваш номер студенческого билета
PARAM_R = 0.60 + (STUDENT_ID % 7) * 0.10   # Базовый радиус / масштаб (м): 0.60 ... 1.20
PARAM_V = 0.20 + (STUDENT_ID % 5) * 0.05   # Линейная скорость (м/с): 0.20 ... 0.40
PARAM_L = 0.25 + (STUDENT_ID % 4) * 0.05   # Дистанция отступа / упреждения (м): 0.25 ... 0.40
```

### Список вариантов Модуля 1:

| Вариант | Название задания | Сенсорный вход | Алгоритмическая задача студента (самописная реализация) |
|:---:|:---|:---|:---|
| **1** | **Следование по массиву путевых точек (Waypoint Tracking)** | `/odom` | Последовательный объезд 4 путевых точек $[(0,0), (P_x, 0), (P_x, P_y), (0, P_y)]$, где $P_x = \text{PARAM\_R}, P_y = \text{PARAM\_L}$: расчет угла рассогласования $\alpha = \text{atan2}(\Delta y, \Delta x) - \theta$, пропорциональный доворот и переключение на следующую точку при приближении ближе 5 см. |
| **2** | **ПИД-регулятор движения по окружности (Circular Motion PID)** | `/odom` | Движение тележки строго по окружности радиуса `PARAM_R` со скоростью `PARAM_V`. ПИД-контур непрерывно корректирует угловую скорость $\omega = \omega_0 + \Delta\omega$ по ошибке текущего радиуса: $e(t) = \sqrt{x^2 + y^2} - \text{PARAM\_R}$. |
| **3** | **П-регулятор точного заезда в целевую позу (Go-to-Goal & Heading)** | `/odom` | Двухэтапный пропорциональный регулятор: поворот на цель $(x_g, y_g) = (\text{PARAM\_R}, \text{PARAM\_L})$, прямолинейный доезд с плавным замедлением и финальный разворот на требуемый курс $\theta_g = 90^\circ$ с точностью по углу $\le 3^\circ$. |
| **4** | **ПИД-движение вдоль стены по лидару (Wall Follower)** | `/scan` | ПИД-регулятор угловой скорости $\omega$ по лучам бокового сектора лидара: измерение минимальной дистанции до стены и строгое поддержание бокового зазора `PARAM_L` при скорости движения `PARAM_V`. |
| **5** | **Трапецеидальный профиль скорости (Trapezoidal Velocity Profile)** | `/odom` | Самописный генератор плавного разгона и торможения на дистанцию $S = 2 \cdot \text{PARAM\_R}$: фаза разгона с ускорением $+a$, фаза маршевой скорости `PARAM_V` и фаза торможения $-a$ с остановкой ровно в целевой точке без рывков. |
| **6** | **Следование за лидером на дистанции (Distance Keeping)** | TF2 буфер | Чтение взаимного положения роботов из дерева TF2 (`leader -> base_footprint`). П-регулятор линейной скорости ведомого робота для строгого удержания дистанции `PARAM_L` позади ведущего. |
| **7** | **Интегрирование дифференциальной одометрии (Dead Reckoning)** | `/joint_states` | Численное интегрирование позы робота по тикам колес (метод средней точки): расчет линейного $\Delta s = R(\Delta\theta_r + \Delta\theta_l)/2$ и углового $\Delta\theta = R(\Delta\theta_r - \Delta\theta_l)/B$ смещения, трансляция фрейма `my_odom` и сопоставление дрейфа с `/odom`. |
| **8** | **Реактивный объезд препятствий (Obstacle Avoidance)** | `/scan`, `/odom` | Движение к цели с реактивным отталкиванием: при обнаружении препятствия в переднем секторе лидара ближе $0.6$ м робот пропорционально снижает скорость и плавно отворачивает в сторону с максимальным свободным расстоянием. |
| **9** | **Движение по синусоидальной траектории (Sinusoidal Path)** | `/odom` | Аналитическое управление дифференциальной тележкой для движения по синусоиде $y(t) = \text{PARAM\_L} \cdot \sin(\frac{2\pi}{\text{PARAM\_R}} x)$ с постоянной продольной скоростью `PARAM_V` и расчетом требуемой угловой скорости $\omega(t)$. |
| **10** | **Экстренное торможение перед препятствием (Emergency Braking)** | `/scan`, `/odom` | Движение со скоростью `PARAM_V`. Мониторинг фронтального сектора лидара ($\pm 15^\circ$): при появлении объекта вычисление дистанции торможения $S_{stop} = v^2 / (2 a_{max})$ и плавная остановка робота ровно в $15$ см от преграды. |
| **11** | **Объезд прямоугольного периметра (Polygon Patrol)** | `/odom` | Точный объезд прямоугольника размером $\text{PARAM\_R} \times \text{PARAM\_L}$ по одометрии: чередование прямолинейного движения, полной остановки и поворота на месте ровно на $90^\circ$ с возвратом в исходную точку $(0,0)$. |
| **12** | **Лидарное следование за подвижным объектом (Target Follower)** | `/scan` | Выделение ближайшего объекта в секторе $\pm 60^\circ$ лидара, расчет дистанции $r$ и азимута $\varphi$. ПД-регулятор угловой и линейной скорости для плавного следования за объектом на дистанции `PARAM_L` при скорости ведущего до `PARAM_V`. |
| **13** | **Конечный автомат пошаговых маневров (FSM Motion Sequencer)** | `/odom` | Реализация логики через FSM: 1) Плавный разгон до `PARAM_V`, 2) Движение вперед на дистанцию `PARAM_R`, 3) Остановка и пауза 2 секунды, 4) Разворот на месте на $180^\circ$, 5) Возврат в исходную точку и парковка. |
| **14** | **Движение по спирали с расширением радиуса (Expanding Spiral)** | `/odom` | Генерация движения по раскручивающейся спирали $r(t) = r_0 + k \cdot t$: непрерывный пересчет линейной скорости $v$ и угловой скорости $\omega(t) = v / r(t)$ для равномерного покрытия площади без проскальзывания колес. |
| **15** | **ROS 2 Сервис дискретных перемещений (Motion Command Service)** | `/odom` | Сервер пользовательского сервиса для базовых команд `MoveDistance(distance, speed)` и `RotateAngle(angle, angular_speed)`: точная отработка команд по одометрии с замедлением в конце и отправкой ответа `success: True`. |

---

# 🤖 Блок заданий Модуля 2 (по материалам Семинаров 2 и 3)

**Базовая платформа:** Мобильный манипулятор KUKA YouBot ([`youbot_description`](src/youbot_description/)) — омни-база (Mecanum) + 5-DOF манипулятор.  
**Среда выполнения:** ROS 2 Humble, `rclpy`, Gazebo Classic (физика ODE, гравитация), RViz2.  
**Основные топики робота:** `/youbot/cmd_vel` (скорость базы), `/youbot/odom` (одометрия), `/youbot/set_joint_trajectory` (управление рукой с `frame_id: 'world'`), `/youbot/scan` (лидар), `/youbot/joint_states` (состояния шарниров).

> [!IMPORTANT]
> **Принцип выполнения:** Все сценарии управления базой и сочленениями руки реализуются студентом **самостоятельно с нуля на Python** на основе материалов Семинаров 2 и 3 (без сложных матричных преобразований высшего порядка, якобианов и внешних пакетов). Логика строится на базовой геометрии, омни-маневрах, интерполяции углов и конечных автоматах.

### Индивидуальные параметры (рассчитываются каждым студентом):
```python
STUDENT_ID = 20240185  # Ваш номер студенческого билета
ARM_ANGLE_DEG = 15.0 + (STUDENT_ID % 6) * 5.0    # Угловая амплитуда / смещение руки (град): 15 ... 40
BASE_SPEED    = 0.20 + (STUDENT_ID % 4) * 0.05   # Скорость омни-базы (м/с): 0.20 ... 0.35
TARGET_DIST   = 0.80 + (STUDENT_ID % 5) * 0.10   # Базовая дистанция / радиус маневра (м): 0.80 ... 1.20
```

### Список вариантов Модуля 2:

| Вариант | Название задания | Исполнительный узел | Алгоритмическая задача студента (самописная реализация) |
|:---:|:---|:---|:---|
| **1** | **Программный секвенсор поз манипулятора (Keyframe Sequencer)** | Рука YouBot | Пошаговый циклический перевод руки через 4 ключевые позы, заданные в градусах (парковочная $\to$ готов к работе $\to$ наклон вперед $\to$ подъем), с расчетом шага интерполяции и плавной сменой позы каждые 3 секунды. |
| **2** | **Омни-маневр: проезд квадрата без разворота (Mecanum Strafe)** | База YouBot | Проезд роботом периметра квадрата $\text{TARGET\_DIST} \times \text{TARGET\_DIST}$ без поворота корпуса (вперед со скоростью `BASE_SPEED`, боком вправо, назад и боком влево) с контролем пути по `/youbot/odom` при $\omega_z = 0$. |
| **3** | **Орбитальный обход препятствия боком (Mecanum Orbiting)** | База YouBot | Движение базы YouBot по окружности радиуса `TARGET_DIST` со скоростью `BASE_SPEED` с непрерывным направлением оси $X$ в центр за счет сложения боковой скорости $V_y = \text{BASE\_SPEED}$ и вращения $\omega_z = V_y / R$. |
| **4** | **Гармоническая омни-траектория «Восьмерка» (Figure-8)** | База YouBot | Движение базы по траектории «восьмерка» за счет одновременной подачи гармонических скоростей $V_x(t) = \text{BASE\_SPEED}\cos(\omega t)$ и $V_y(t) = \text{BASE\_SPEED}\sin(2\omega t)$ с нулевой угловой скоростью $\omega_z = 0$. |
| **5** | **Наведение манипулятора на азимут цели (Arm Yaw Targeting)** | Рука YouBot | По заданным координатам цели $(x_g, y_g)$ расчет угла первого сустава $q_1 = \text{atan2}(y_g, x_g)$, плавный доворот сочленения `arm_joint_1` на рассчитанный угол и фиксация позы в симуляторе. |
| **6** | **Плавная S-образная интерполяция суставов (Smoothstep)** | Рука YouBot | Реализация функции гладкого перехода $s(\tau) = 3\tau^2 - 2\tau^3$ (где $\tau = t/T \in [0, 1]$) вместо линейной интерполяции для устранения динамических рывков звеньев при старте и торможении руки YouBot. |
| **7** | **Конечный автомат операции Pick & Place (FSM Pick & Place)** | База + Рука | Пошаговый FSM: 1) Подъезд базы вперед на расстояние `TARGET_DIST` по одометрии $\to$ 2) Опускание руки в позу захвата $\to$ 3) Пауза/сведение схвата $\to$ 4) Подъем руки $\to$ 5) Откат базы назад на `TARGET_DIST`. |
| **8** | **Горизонтирование схвата манипулятора (End-Effector Leveling)** | Рука YouBot | Узел изменяет углы плеча $q_2$ и локтя $q_3$ с шагом `ARM_ANGLE_DEG`, при этом угол запястья автоматически рассчитывается как $q_4 = -(q_2 + q_3)$, удерживая губки схвата строго параллельно плоскости пола. |
| **9** | **Челночное диагональное движение «Зигзаг» (Omni Zigzag)** | База YouBot | Поочередное омни-движение под углом $45^\circ$ вперед-вправо ($V_x = V, V_y = -V$) и вперед-влево ($V_x = V, V_y = V$) на расстояние `TARGET_DIST` с контролем пройденного пути по одометрии. |
| **10** | **Синхронный старт: движение платформы с раскладыванием руки** | База + Рука | При подаче скорости на базу $V_x = \text{BASE\_SPEED}$ манипулятор синхронно раскладывается из компактной позы в рабочую; при остановке базы рука плавно складывается обратно в исходное положение. |
| **11** | **Безопасная лидарная остановка YouBot (Lidar Safety Brake)** | База YouBot | Движение базы YouBot со скоростью `BASE_SPEED`. Мониторинг фронтального сектора лидара `/youbot/scan` ($\pm 25^\circ$): при обнаружении препятствия ближе $0.6$ м платформа плавно сбрасывает скорость до нуля. |
| **12** | **Калибровка пройденного расстояния омни-базы (Odometry Calibration)** | База YouBot | Прямолинейный заезд на расчетную дистанцию `TARGET_DIST`, сопоставление фактических координат по `/youbot/odom` с теоретическими и расчет масштабного поправочного коэффициента $k = S_{fact} / S_{theor}$. |
| **13** | **Программный фильтр допустимых углов руки (Joint Limits Guard)** | Рука YouBot | Узел-фильтр для траекторий манипулятора: проверка задаваемых углов на непревышение паспортных пределов YouBot из Семинара 3 ($q_{min} \le q \le q_{max}$) с автоматическим ограничением (clamping) и логированием предупреждений. |
| **14** | **Маятниковое гармоническое качание звеньев (Harmonic Motion)** | Рука YouBot | Формирование непрерывных синусоидальных колебаний плеча и локтя: $q_2(t) = q_{2,0} + A \sin(\omega t)$, $q_3(t) = q_{3,0} + A \cos(\omega t)$ с амплитудой `ARM_ANGLE_DEG` без превышения пределов суставов. |
| **15** | **ROS 2 Сервис пресетов конфигураций руки (Arm Preset Poses)** | Рука YouBot | Сервер пользовательского сервиса с именованными позами (`HOME`, `TRANSPORT`, `EXTENDED`, `PICK`), обеспечивающий плавный интерполированный перевод звеньев между заданными конфигурациями. |

---

## 📮 Порядок отправки на проверку

1. Подготовить архив или репозиторий со следующими материалами:
   * **Исходный код узла:** Python-скрипт (или C++ пакет), реализующий алгоритм.
   * **Запись прогона симулятора:** Файл `rosbag2_*.db3` (или `.mcap`) длительностью не менее 30 секунд.
   * **Краткий отчет (PDF или Markdown):**
     * Номер зачетки (`STUDENT_ID`) и рассчитанные по нему индивидуальные параметры.
     * Математическое описание алгоритма с формулами.
     * Скриншоты работающей симуляции в Gazebo/RViz2.
2. Отправить материалы преподавателю на почту: **`mail@borislap.ru`** с темой письма вида:  
   `[ROS2-2026] Модуль 1 (или 2) - Индивидуальное задание - Вариант №X - Фамилия Имя`.
