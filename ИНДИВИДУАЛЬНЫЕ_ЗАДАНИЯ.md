# Индивидуальные практические задания (54 балла)

В данном документе приведены темы и требования к индивидуальным заданиям по курсу робототехники в ROS 2 Humble. Успешное выполнение индивидуального задания оценивается в **54 балла** за соответствующий модуль и освобождает от сдачи базовых лабораторных отчетов по семинарам этого модуля.

---

## Регламент оценивания

Семестровый курс разделен на два контрольных модуля:
* **Модуль 1 (Семинары 0 и 1):** мобильная колесная робототехника, основы Linux, Docker, граф вычислений ROS 2, моделирование URDF и TF2.
  * Базовый трек: отчеты по Семинарам 0 и 1 (средний балл до 45).
  * Индивидуальное задание Модуля 1: 54 балла за модуль.
* **Модуль 2 (Семинары 2 и 3):** физическая симуляция в Gazebo Classic, омни-платформа Mecanum, 5-осевой манипулятор KUKA YouBot.
  * Базовый трек: отчеты по Семинарам 2 и 3 (средний балл до 45).
  * Индивидуальное задание Модуля 2: 54 балла за модуль.

Итоговая оценка за семестр формируется как среднее арифметическое оценок за Модуль 1 и Модуль 2. Для получения 54 баллов за семестр студент сдает 2 индивидуальных проекта: один по Модулю 1 и один по Модулю 2.

---

## Правила выбора варианта и требования к выполнению

1. **Выбор варианта:**
   Рекомендуемый способ выбора уникального варианта — по номеру студенческого билета / зачетной книжки (`STUDENT_ID`):
   $$\text{Вариант} = (\text{STUDENT\_ID} \pmod{15}) + 1$$
   Индивидуальные числовые параметры алгоритма рассчитываются по формулам, приведенным в описании каждого модуля.

2. **Самостоятельность выполнения:**
   Все задания должны быть выполнены строго самостоятельно. Задания, выполненные не самостоятельно, отклоняются.

3. **Материалы для сдачи:**
   * Исходный код разработанного узла на Python (`rclpy`).
   * Запись топиков симуляции ROS 2 Bag (`.db3` или `.mcap`) длительностью не менее 30 секунд:
     ```bash
     ros2 bag record /tf /clock /cmd_vel /odom /youbot/joint_states -o my_run.bag
     ```
   * Краткий отчет (PDF или Markdown) с формулами, описанием логики и скриншотами симуляции в Gazebo/RViz2.

---

## Модуль 1. Мобильные колесные роботы (по материалам Семинаров 1 и 2)

**Базовая платформа:** Дифференциальный колесный робот ([`basic_mobile_robot`](src/basic_mobile_robot/)) в ROS 2 и Gazebo Classic.  
**Среда выполнения:** ROS 2 Humble, `rclpy`, RViz2, Gazebo Classic.  
**Основные топики робота:** `/cmd_vel` (скорость тележки), `/odom` (одометрия), `/scan` (2D-лидар), `/joint_states` (состояния колес), `/tf` (координатные фреймы).

Все алгоритмы реализуются студентом самостоятельно с нуля на Python (`rclpy` + `math`/`numpy`) без использования готовых высокоуровневых пакетов навигации (Nav2 и т.д.).

### Расчет индивидуальных параметров:
```python
STUDENT_ID = 20240185  # Ваш номер студенческого билета
PARAM_R = 0.60 + (STUDENT_ID % 7) * 0.10   # Базовый радиус / масштаб (м): 0.60 ... 1.20
PARAM_V = 0.20 + (STUDENT_ID % 5) * 0.05   # Линейная скорость (м/с): 0.20 ... 0.40
PARAM_L = 0.25 + (STUDENT_ID % 4) * 0.05   # Дистанция отступа / упреждения (м): 0.25 ... 0.40
```

### Список вариантов Модуля 1:

| Вариант | Название задания | Сенсорный вход | Алгоритмическая задача студента (самописная реализация) |
|:---:|:---|:---|:---|
| **1** | **Плавное следование по путевым точкам (Smooth Waypoints)** | `/odom` | Последовательный объезд 3 путевых точек $[(P_x, 0), (P_x, P_y), (0, 0)]$, где $P_x = \text{PARAM\_R}, P_y = \text{PARAM\_L}$. Замкнутый контур: расчет курсового рассогласования $\Delta\theta = \text{atan2}(\Delta y, \Delta x) - \theta$ с нормализацией в $[-\pi, \pi]$, П-регулятор угловой скорости и П-регулятор линейной скорости от остаточного расстояния для плавного торможения в пределах 5 см от каждой точки. |
| **2** | **ПИД-регулятор движения по окружности (Orbital PID)** | `/odom` | Движение по окружности радиуса `PARAM_R` вокруг начальной точки $(0, 0)$. Начальный выезд на радиус, затем включение контура ПИД-регулирования угловой скорости $\omega(t) = \frac{V}{R} + \Delta\omega_{PID}$ по ошибке радиуса $e(t) = \text{PARAM\_R} - \sqrt{x^2 + y^2}$ при тангенциальной скорости `PARAM_V`. Автоматическая плавная остановка ровно после завершения одного полного витка ($360^\circ$) с печатью остаточной радиальной ошибки. |
| **3** | **ПД-регулятор точного заезда и ориентации (Docking PD)** | `/odom` | Двухконтурный ПД-регулятор для точного заезда в целевую точку $(x_g, y_g) = (\text{PARAM\_R}, \text{PARAM\_L})$ с финальным разворотом на курс $\theta_g = 90^\circ$. ПД-закон по линейной скорости $v = K_{p,v} d - K_{d,v} \dot{d}$ и курсу $\omega = K_{p,\theta} e_\theta - K_{d,\theta} \dot{e}_\theta$ для устранения колебаний массивного робота. Остановка при точности по координатам $< 3$ см и по углу $< 2^\circ$. |
| **4** | **Фильтрованный лидарный Wall Follower (Filtered Lidar PID)** | `/scan`, `/odom` | Движение вдоль стены со скоростью `PARAM_V`. Выделение бокового сектора лидара ($60^\circ \dots 120^\circ$), устранение шума измерений (медианный фильтр по 5 соседним лучам) и ПД-регулирование $\omega_z$ для строгого удержания зазора `PARAM_L`. Интегрирование пройденного пути по `/odom` и автоматический контролируемый останов ровно после 4.0 метров движения. |
| **5** | **Замкнутое слежение за профилем скорости (Closed-Loop Profile)** | `/odom` | Аналитический расчет опорной траектории $x_{ref}(t), v_{ref}(t)$ с трапецеидальным профилем скорости (разгон с ускорением $a = 0.2\ \text{м/с}^2$, марш со скоростью `PARAM_V`, торможение) на дистанцию $S = 2 \cdot \text{PARAM\_R}$. Контур обратной связи: $v(t) = v_{ref}(t) + K_p (x_{ref}(t) - x(t))$ для компенсации инерции тяжелых колес. Остановка с ошибкой $< 3$ см. |
| **6** | **TF2 Следование за подвижным ориентиром (Virtual Target)** | TF2, `/odom` | Узел транслирует через `tf2_ros.TransformBroadcaster` динамический целевой фрейм `target_frame`, движущийся по окружности радиуса `PARAM_R`. С помощью `tf2_ros.Buffer` узел считывает трансформацию `base_footprint -> target_frame` и отрабатывает П-регулятором линейную и угловую скорость для удержания дистанции `PARAM_L` до цели в течение 30 секунд. |
| **7** | **Интегрирование кинематики и верификация (Dead Reckoning)** | `/joint_states`, `/odom` | Узел ведет робота по квадрату со стороной `PARAM_R` (П-регуляторы дистанции и углов). Одновременно по `/joint_states` численно интегрируется собственная одометрия робота (метод средней точки) с трансляцией TF-фрейма `my_odom`. По возвращении в исходную точку — расчет евклидовой ошибки между эталоном `/odom` и вычисленной позой. |
| **8** | **Навигация методом потенциальных полей (Potential Fields)** | `/scan`, `/odom` | Движение к цели $(x_g, y_g) = (\text{PARAM\_R}, \text{PARAM\_L})$. Расчет результирующего вектора скорости $\vec{V} = \vec{F}_{att} + \vec{F}_{rep}$, где $\vec{F}_{att}$ — притяжение к цели, а $\vec{F}_{rep}$ — отталкивание от точек лидара ближе $0.8$ м. Преобразование вектора в команды $(v, \omega)$ дифференциального привода. Остановка в радиусе 5 см от цели без коллизий. |
| **9** | **Следование по синусоиде алгоритмом Pure Pursuit** | `/odom` | Движение вдоль траектории $y(x) = \text{PARAM\_L} \cdot \sin(\frac{2\pi}{\text{PARAM\_R}} x)$. Реализация классического алгоритма Pure Pursuit: поиск точки прицеливания на траектории на дистанции упреждения $L_{ld}$, расчет кривизны $\kappa = \frac{2 \Delta y}{L_{ld}^2}$ и угловой скорости $\omega = \text{PARAM\_V} \cdot \kappa$. Остановка после завершения одного полного периода синусоиды. |
| **10** | **Адаптивный круиз-контроль с оценкой скорости (ACC)** | `/scan`, `/odom` | Движение со скоростью `PARAM_V`. Мониторинг расстояния до впереди идущего препятствия в секторе $\pm 15^\circ$ лидара. Численное дифференцирование дистанции $\dot{d} = \frac{\Delta d}{\Delta t}$ для оценки относительной скорости. ПД-регулятор дистанции: плавное замедление и удержание дистанции `PARAM_L` при движении объекта или полная остановка с зазором $\pm 3$ см при его неподвижности. |
| **11** | **Замкнутое патрулирование полигона (Polygon Patrol)** | `/odom` | Проезд периметра треугольника или прямоугольника со стороной `PARAM_R` в замкнутом контуре по `/odom`. П-регулятор дистанции на прямых участках, полная остановка и точный разворот на месте на заданный угол с компенсацией накопленной угловой ошибки. Критерий завершения: возврат в точку старта $(0, 0)$ с остаточной ошибкой $< 5$ см. |
| **12** | **Лидарный захват и сопровождение цели (Lidar Target Tracking)** | `/scan`, `/odom` | Выделение связного кластера точек ближайшего объекта в секторе лидара, расчет его полярных координат $(r, \varphi)$. Два параллельных контура регулирования: угловой ПД-регулятор держит объект по курсу ($\varphi \to 0$), а линейный П-регулятор удерживает дистанцию `PARAM_L` при скорости до `PARAM_V`. Устойчивое удержание цели в течение 20 с. |
| **13** | **FSM-маневр параллельной парковки (FSM Parallel Parking)** | `/odom`, `/scan` | Конечный автомат из 4 состояний: 1) Проезд вперед на дистанцию `PARAM_R` вдоль стены; 2) Лидарное обнаружение парковочного кармана глубиной $\ge \text{PARAM\_L}$; 3) Заезд задним ходом под углом $45^\circ$ с контролем дистанции; 4) Выравнивание корпуса параллельно стене. Переход между состояниями строго по одометрии и лидару. |
| **14** | **Спираль Архимеда с коррекцией бокового сноса (Archimedean Spiral)** | `/odom` | Движение по раскручивающейся спирали $r(\theta) = r_0 + k \theta$. Расчет текущей полярной ошибки робота относительно теоретической спирали по координатам `/odom` и П-коррекция угловой скорости $\omega = \omega_{theor} + K_p e_r$. Плавный останов робота при достижении граничного радиуса `PARAM_R` и печать среднеквадратичного отклонения. |
| **15** | **Неблокирующий сервис пошаговых маневров (Non-blocking Service)** | `/odom` | Сервер сервиса (`std_srvs/srv/Trigger`), запускающий автономный калиброванный маневр: разгон, прямолинейный проезд `PARAM_R`, разворот на $180^\circ$ и возврат в $(0, 0)$. Коллбек сервиса не блокирует исполнение (non-blocking FSM в таймере узла), возвращает подтверждение старта, а робот отрабатывает маневр с точностью возврата $< 5$ см. |

---

## Модуль 2. Мобильный манипулятор KUKA YouBot (по материалам Семинаров 2 и 3)

**Базовая платформа:** Мобильный манипулятор KUKA YouBot ([`youbot_description`](src/youbot_description/)) — омни-база (Mecanum) + 5-DOF манипулятор.  
**Среда выполнения:** ROS 2 Humble, `rclpy`, Gazebo Classic (физика ODE, гравитация), RViz2.  
**Основные топики робота:** `/youbot/cmd_vel` (скорость базы), `/youbot/odom` (одометрия), `/youbot/set_joint_trajectory` (управление рукой с `frame_id: 'world'`), `/youbot/scan` (лидар), `/youbot/joint_states` (состояния шарниров).

Все алгоритмы управления омни-базой и сочленениями руки реализуются студентом самостоятельно с нуля на Python (`rclpy` + `math`/`numpy`).

### Расчет индивидуальных параметров:
```python
STUDENT_ID = 20240185  # Ваш номер студенческого билета
ARM_ANGLE_DEG = 15.0 + (STUDENT_ID % 6) * 5.0    # Угловая амплитуда / смещение руки (град): 15 ... 40
BASE_SPEED    = 0.20 + (STUDENT_ID % 4) * 0.05   # Скорость омни-базы (м/с): 0.20 ... 0.35
TARGET_DIST   = 0.80 + (STUDENT_ID % 5) * 0.10   # Базовая дистанция / радиус маневра (м): 0.80 ... 1.20
```

### Список вариантов Модуля 2:

| Вариант | Название задания | Исполнительный узел | Алгоритмическая задача студента (самописная реализация) |
|:---:|:---|:---|:---|
| **1** | **Секвенсор поз с верификацией сходимости (Keyframe Verifier)** | Рука YouBot | Циклический перевод манипулятора через 4 ключевые позы (Home $\to$ Reach $\to$ Dip $\to$ Lift) с параметром `ARM_ANGLE_DEG`. Генерация точек `JointTrajectory` с интерполяцией. Непрерывный опрос `/youbot/joint_states`: переход к следующей позе происходит только по факту сходимости всех суставов (ошибка $< 0.05$ рад в течение 0.5 с). Выполнение 2 циклов и парковка. |
| **2** | **Mecanum-квадрат в замкнутом контуре (Closed-loop Strafe)** | База YouBot | Проезд периметра квадрата $\text{TARGET\_DIST} \times \text{TARGET\_DIST}$ без разворота корпуса (последовательный стрейф вперед, вправо, назад, влево). Замкнутый контур по `/youbot/odom`: П-регуляторы линейных скоростей $V_x, V_y$ и регулятор угловой скорости $\omega_z = -K_p \theta_z$ для парирования паразитного проскальзывания роликов. Возврат в $(0, 0)$ с ошибкой $< 3$ см. |
| **3** | **Лидарный орбитальный обход (Lidar Closed-loop Orbiting)** | База YouBot | Облет препятствия омни-базой по кругу боком со скоростью `BASE_SPEED` с непрерывным направлением носовой оси робота на объект. Замкнутый контур: касательная скорость $V_y = \text{BASE\_SPEED}$, угловая скорость $\omega_z = V_y / \text{TARGET\_DIST}$, а радиальная скорость $V_x$ корректируется ПИД-регулятором по лидару `/youbot/scan` для строгого удержания дистанции `TARGET_DIST`. |
| **4** | **Омни-траектория «Восьмерка» с коррекцией (Figure-8 Tracking)** | База YouBot | Движение базы по плоской лемнискате с масштабом `TARGET_DIST`. Генерация опорных координат $(x_{ref}(t), y_{ref}(t))$ и П-регулирование скоростей $(V_x, V_y)$ по ошибке текущей одометрии `/youbot/odom` при $\omega_z = 0$. Контролируемая остановка ровно после завершения одной полной траектории с ошибкой в стартовой точке $< 5$ см. |
| **5** | **2-DOF слежение манипулятором по лидару (Arm Lidar Tracking)** | Рука YouBot | Наведение манипулятора на ближайшее препятствие в реальном времени. По координатам объекта из `/youbot/scan` рассчитывается азимут $q_1 = \text{atan2}(y, x)$ для поворотного шарнира `arm_joint_1` и угол наклона плеча $q_2 = f(r)$ для `arm_joint_2`. Плавная интерполяция и отслеживание перемещения препятствия в течение 20 с точностью $< 3^\circ$. |
| **6** | **Плавный Smoothstep-профиль с оценкой рассогласования** | Рука YouBot | Перевод звеньев `arm_joint_2` и `arm_joint_3` на угол `ARM_ANGLE_DEG` с использованием S-образной кубической кривой $s(\tau) = 3\tau^2 - 2\tau^3$ вместо линейной интерполяции. Подписка на `/youbot/joint_states`, расчет динамического отставания реальных углов от командных (из-за гравитации в Gazebo) и логирование графика/значений ошибки. |
| **7** | **Сценарий скоординированной манипуляции (Approach & Touch)** | База + Рука | Трехстадийный алгоритм: 1) Подъезд платформы к целевой отметке на расстояние `TARGET_DIST` по `/youbot/odom` в замкнутом контуре; 2) Полная остановка базы ($V < 0.01$ м/с) и плавное опускание руки YouBot в рабочую позу взаимодействия; 3) Выдержка 3 секунды, возврат руки в позу Home и откат платформы назад в точку старта. |
| **8** | **Активное горизонтирование схвата при движении платформы** | База + Рука | Платформа совершает возвратно-поступательное омни-движение вперед-назад на 1 метр со скоростью `BASE_SPEED`. Одновременно звенья плеча $q_2$ и локтя $q_3$ совершают непрерывные качания с амплитудой `ARM_ANGLE_DEG`. Алгоритм в реальном времени вычисляет $q_4(t) = -(q_2(t) + q_3(t))$, удерживая схват строго горизонтально (отклонение фланца $< 2^\circ$). |
| **9** | **Реактивный омни-зигзаг по лидару (Reactive Omni Zigzag)** | База YouBot | Диагональное челночное омни-движение базы ($V_x = V, V_y = V$) со скоростью `BASE_SPEED` без разворота корпуса. При обнаружении преграды лидаром `/youbot/scan` ближе 0.6 м узел плавно переключает поперечную скорость на противоположную ($V_y = -V$). Выполнение 4 контролируемых отскоков и остановка с контролем пройденного пути. |
| **10** | **Скоростное синхронное раскладывание руки (Velocity-Coupled Deploy)** | База + Рука | Разгон базы по прямолинейной траектории на дистанцию `TARGET_DIST`. Степень раскрытия манипулятора ($q_2, q_3$) динамически синхронизирована с текущей линейной скоростью базы: $q_i(t) = q_{i,0} + \Delta q \cdot (v_x(t) / V_{max})$. При торможении базы рука синхронно складывается обратно в Home. Остановка без раскачки звеньев. |
| **11** | **Скоординированная аварийная защита (Emergency Shielding)** | База + Рука | Платформа движется со скоростью `BASE_SPEED` с выдвинутым вперед манипулятором. Мониторинг фронтального сектора лидара `/youbot/scan` ($\pm 25^\circ$). При обнаружении преграды ближе $0.8$ м узел запускает координированное торможение базы и экстренное втягивание руки в компактную защитную позу до полной остановки перед препятствием. |
| **12** | **Калибровка проскальзывания Mecanum с точным возвратом** | База YouBot | Движение базы вперед до достижения дистанции 0.5 м до стены по показаниям лидара `/youbot/scan`. Сравнение пройденного пути по одометрии $S_{odom}$ и по лидару $S_{lidar}$, расчет коэффициента скольжения $K_{slip} = S_{lidar} / S_{odom}$. Применение калибровочной поправки для автономного возврата назад строго в начальную точку $(0, 0)$ с ошибкой $< 3$ см. |
| **13** | **Адаптивные лимиты суставов по лидару (Obstacle-Aware Guard)** | Рука YouBot | Манипулятор совершает непрерывное угловое сканирование шарниром `arm_joint_1` в секторе $\pm 60^\circ$. При появлении препятствия в боковых секторах лидара алгоритм на лету динамически пересчитывает безопасный диапазон сустава $[q_{min}^{dyn}, q_{max}^{dyn}]$, предотвращая столкновение руки с объектом и мягко реверсируя движение на границе. |
| **14** | **Синфазное гармоническое движение и компенсация фокуса** | База + Рука | Платформа совершает поперечные гармонические колебания $V_y(t) = \text{BASE\_SPEED}\sin(\omega t)$. Шарнир основания манипулятора `arm_joint_1` синфазно отрабатывает компенсирующий угол $q_1(t) = -\text{atan2}(y(t), \text{TARGET\_DIST})$, удерживая направление рабочего органа на фиксированную точку пространства. Контроль дрейфа точки прицеливания. |
| **15** | **Неблокирующий сервис пресетов с верификацией сходимости** | Рука YouBot | Сервер сервиса (`std_srvs/srv/Trigger`), запускающий перевод манипулятора в рабочую конфигурацию с угловым смещением `ARM_ANGLE_DEG`. Неблокирующий коллбек передает команду в таймерный генератор траектории; сервис возвращает подтверждение завершения только после фактического выхода суставов в цель по `/youbot/joint_states` ($\pm 0.03$ рад). |

---

## Порядок сдачи работы

1. Подготовить архив или репозиторий со следующими материалами:
   * **Исходный код узла:** Python-скрипт (или C++ пакет), реализующий алгоритм.
   * **Запись прогона симулятора:** Файл `rosbag2_*.db3` (или `.mcap`) длительностью не менее 30 секунд.
   * **Краткий отчет (PDF или Markdown):**
     * Номер студенческого билета (`STUDENT_ID`) и рассчитанные по нему индивидуальные параметры.
     * Описание алгоритма с формулами.
     * Скриншоты работающей симуляции в Gazebo/RViz2.
2. Отправить материалы преподавателю на почту: **`mail@borislap.ru`** с темой письма вида:  
   `[ROS2-2026] Модуль 1 (или 2) - Индивидуальное задание - Вариант №X - Фамилия Имя`.
